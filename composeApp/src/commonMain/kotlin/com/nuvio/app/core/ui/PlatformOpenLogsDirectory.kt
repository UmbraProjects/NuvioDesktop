package com.nuvio.app.core.ui

/**
 * Opens the app's logs directory in the platform file manager so users can grab
 * nuvio.log / nuvio-mpv.log without hunting for the AppData path themselves.
 */
expect fun platformOpenLogsDirectory()
